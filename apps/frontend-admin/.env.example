# VULTRA Admin Portal — Environment Variables
# Copy to .env.local and fill in values

# URL of the API Core (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Next.js dev port (default 3001 to avoid clash with API Core on 3000)
# Start with: npm run dev -- -p 3001
